<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mycompany.urihome_mini_web.dao.RecipientDao">
	<insert id="order" parameterType="Recipient">
		insert into recipient(onumber, rname, rphone, rzip, raddress, rdetailAddress)
		values(#{onumber}, #{rname}, #{rphone}, #{rzip}, #{raddress}, #{rdetailAddress})
	</insert>
</mapper>